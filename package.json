{
    "name": "ws-setup",
    "description": "WhatSlack setup command",
    "version": "0.1.0",
    "private": true,
    "author": "Kristof Dewilde <git@krivi.be>",
    "scripts": {
        "setup-content": "cd develop/content && npm install && npm run build && cd ../../",
        "setup-options": "cd develop/options && npm install && npm run build && cd ../../",
        "setup": "npm run setup-content && npm run setup-options"
    }
}
